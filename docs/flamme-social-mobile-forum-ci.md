# Flamme Social CI check

Temporary validation branch used to run the repository CI against the current `main` code after the mobile navigation, forum discussions, and Flamme engine shortcut changes.
