const form=document.getElementById('search');const q=document.getElementById('q');form.addEventListener('submit',e=>{e.preventDefault();const raw=q.value.trim();if(!raw)return;let target;if(/^https?:\/\//i.test(raw)){target=raw;}else if(/^[\w.-]+\.[a-z]{2,}(\/.*)?$/i.test(raw)&&!raw.includes(' ')){target='https://'+raw;}else{target='https://www.google.com/search?q='+encodeURIComponent(raw);}location.href=target;});
